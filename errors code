#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int n, m, a, su
    cin >> n;
    cin >> m;
    cin >> a;
    if (n > a)
        sum *= (n + a - 1) / a;
    if (m > a
        sum *= (m + a - 1) / a;
    cout << sum;
    return 0;
}
